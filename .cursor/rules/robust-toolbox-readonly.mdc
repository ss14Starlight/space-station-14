---
description: RobustToolbox engine is a separate project and must never be edited
alwaysApply: true
---

# RobustToolbox — Do Not Edit

`RobustToolbox/` is the Robust Toolbox **engine**. It is a **separate upstream project** (typically a git submodule), not BPL14 content.

## Hard rule

- **Never** create, modify, delete, move, or reformat files under `RobustToolbox/`.
- **Never** commit, submodule-bump, vendor, or otherwise update the engine as part of BPL14 work.
- Treat the engine as **read-only**: you may read it for API understanding, but all changes belong in BPL14 content (`Content.*`, `Resources/`, BPL folders, etc.).

If a feature seems to require engine changes, stop and tell the user — do not patch RobustToolbox.
