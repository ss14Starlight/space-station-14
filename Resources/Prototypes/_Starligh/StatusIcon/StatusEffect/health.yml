
# Marker ""icon"" that doesn't actually render but is used to determine if the health bar *should* render.
- type: healthBarIcon
  id: HealthBarMarkerIcon
  icon: # Icon isn't actually used but is a required field.
    sprite: /Textures/Interface/Misc/health_icons.rsi
    state: Fine
