# Soft follow for tutorial mentors: move when out of range, wait when close.
# No idle PickAccessible wander — that BFS path spam is too costly at many concurrent sessions.
- type: htnCompound
  id: TutorialFollowCompound
  branches:
  - tasks:
    - !type:HTNPrimitiveTask
      preconditions:
      - !type:CoordinatesNotInRangePrecondition
        targetKey: FollowTarget
        rangeKey: FollowRange
      operator: !type:MoveToOperator
        pathfindInPlanning: true
        targetKey: FollowTarget
        rangeKey: FollowCloseRange
        removeKeyOnFinish: false
  - tasks:
    - !type:HTNPrimitiveTask
      preconditions:
      - !type:CoordinatesInRangePrecondition
        targetKey: FollowTarget
        rangeKey: FollowRange
      operator: !type:RandomOperator
        targetKey: IdleTime
        minKey: MinimumIdleTime
        maxKey: MaximumIdleTime
    - !type:HTNPrimitiveTask
      preconditions:
      - !type:KeyExistsPrecondition
        key: IdleTime
      - !type:CoordinatesInRangePrecondition
        targetKey: FollowTarget
        rangeKey: FollowRange
      operator: !type:WaitOperator
        key: IdleTime
