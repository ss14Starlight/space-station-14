<controls:FancyWindow xmlns="https://spacestation14.io"
        xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
        Title="{Loc samurai-codes-admin-ui-title}"
        MinSize="560 400">
    <!-->
    this shit does not layout properly unless I put the horizontal boxcontainer inside of a vertical one
    ????
    <!-->
    <BoxContainer Orientation="Vertical">
        <BoxContainer Orientation="Horizontal" Align="End">
            <Button Name="NewCodeButton" Text="{Loc samurai-codes-admin-ui-new-code}" MaxSize="256 64" StyleClasses="OpenRight"/>
            <Button Name="SaveButton" Text="{Loc samurai-codes-admin-ui-save}" MaxSize="256 64" StyleClasses="OpenLeft"/>
        </BoxContainer>
    </BoxContainer>
    <BoxContainer Orientation="Vertical" Margin="4 60 0 0">
        <ScrollContainer VerticalExpand="True" HorizontalExpand="True" HScrollEnabled="False">
            <BoxContainer Orientation="Vertical" Name="CodeContainer" VerticalExpand="True" />
        </ScrollContainer>
    </BoxContainer>
</controls:FancyWindow>
