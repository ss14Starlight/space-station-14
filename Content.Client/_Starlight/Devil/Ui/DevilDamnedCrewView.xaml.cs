using Robust.Client.AutoGenerated;
using Robust.Client.UserInterface;
using Robust.Client.UserInterface.XAML;

namespace Content.Client._Starlight.Devil.Ui;

[GenerateTypedNameReferences]
public sealed partial class DevilDamnedCrewView : Control
{
    public DevilDamnedCrewView((EntityUid, string) subject)
    {
        RobustXamlLoader.Load(this);
        IoCManager.InjectDependencies(this);

        var uid = subject.Item1;
        var name = subject.Item2;

        DamnedSprite.SetEntity(uid);
        DamnedName.Text = name;
    }
}
