<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                      xmlns:customControls="clr-namespace:Content.Client.Administration.UI.CustomControls"
                      xmlns:ui="clr-namespace:Content.Client.Chemistry.UI"
                      Title="{Loc 'reagent-dispenser-bound-user-interface-title'}"
                      MinSize="600 330"
                      SetSize="800 530">
    <BoxContainer Orientation="Vertical">
        <!-- Starlight-start: Top bar with Dispense to container/chemmaster button, Linked Devices button, and is linked label -->
        <BoxContainer Orientation="Horizontal" HorizontalExpand="True" Margin="5">
            <Button Name="ChemMasterToggleButton"
                    Access="Public"
                    Text="{Loc 'reagent-dispenser-chemmaster-toggle-off'}" />
            <Label Name="LinkedChemMasterLabel"
                   Access="Public"
                   Text=""
                   VerticalAlignment="Center"
                   Margin="10 0 0 0"
                   StyleClasses="LabelSubText" />
            <Control HorizontalExpand="True" />
            <Button Name="ChemMasterSelectButton"
                    Access="Public"
                    Text="{Loc 'reagent-dispenser-chemmaster-select-button'}" />
        </BoxContainer>
        <!-- Starlight-end -->
        <BoxContainer Orientation="Horizontal" VerticalExpand="True">
            <BoxContainer Orientation="Vertical" MinWidth="170">
                <Label Text="{Loc 'reagent-dispenser-window-amount-to-dispense-label'}" HorizontalAlignment="Center" />
                <ui:ButtonGrid
                    Name="AmountGrid"
                    Access="Public"
                    Columns="3"
                    HorizontalAlignment="Center"
                    Margin="5"
                    ButtonList="1,5,10,15,20,25,30,50,100"
                    RadioGroup="True">
                </ui:ButtonGrid>
                <!-- Starlight-start -->
                <BoxContainer Orientation="Horizontal" HorizontalExpand="True">
                    <ProgressBar Name="EnergyDisplayBar"
                                    HorizontalExpand="True"
                                    SetHeight="25"
                                    MaxValue="1"
                                    Value="0"
                                    Margin="10">
                        <Label Name="EnergyDisplay"
                                Margin="5 0 0 0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"></Label>
                    </ProgressBar>
                </BoxContainer>
                <!-- Starlight-end -->
            <Control VerticalExpand="True" />
            <Label Name="ContainerInfoName"
                   Access="Public"
                   Text=""
                   HorizontalAlignment="Center" />
            <Label Name="ContainerInfoFill"
                   Access="Public"
                   Text=""
                   HorizontalAlignment="Center"
                   StyleClasses="LabelSubText" />
            <SpriteView Name="View"
                        Scale="4 4"
                        HorizontalAlignment="Center" />
            <BoxContainer Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Name="ClearButton"
                        Access="Public"
                        Text="{Loc 'reagent-dispenser-window-clear-button'}"
                        StyleClasses="OpenRight" />
                <Button Name="EjectButton"
                        Access="Public"
                        Text="{Loc 'reagent-dispenser-window-eject-button'}"
                        StyleClasses="OpenLeft" />
            </BoxContainer>
        </BoxContainer>
        <SplitContainer Orientation="Vertical"
                        HorizontalExpand="True"
                        VerticalExpand="True">
            <ScrollContainer HScrollEnabled="False"
                             HorizontalExpand="True"
                             VerticalExpand="True"
                             MinHeight="50"
                             SizeFlagsStretchRatio="2.5">
                <GridContainer Name="ReagentList"
                               HorizontalExpand="True"
                               VerticalExpand="True"
                               Access="Public"
                               Columns="3" />
            </ScrollContainer>
            <ScrollContainer HScrollEnabled="False"
                             HorizontalExpand="True"
                             VerticalExpand="True"
                             MinHeight="50">
                <PanelContainer VerticalExpand="True">
                    <PanelContainer.PanelOverride>
                        <gfx:StyleBoxFlat BackgroundColor="#1b1b1e" />
                    </PanelContainer.PanelOverride>
                    <BoxContainer Name="ContainerInfo"
                                  Orientation="Vertical"
                                  HorizontalExpand="True"
                                  VerticalExpand="True"
                                  Margin="5">
                        <Label Text="{Loc 'reagent-dispenser-window-no-container-loaded-text'}" />
                    </BoxContainer>
                </PanelContainer>
            </ScrollContainer>
        </SplitContainer>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
