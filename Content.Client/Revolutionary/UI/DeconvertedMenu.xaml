<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                      Title="{Loc 'rev-deconverted-title'}"
                      MinSize="660 340"
                      MaxSize="660 340">
    <BoxContainer Orientation="Vertical" Margin="5">
        <Label Text="{Loc 'rev-deconverted-text'}"/>
        <PanelContainer StyleClasses="BackgroundDark" Margin="0 0 0 10">
            <BoxContainer Orientation="Vertical" Margin="10">
                <RichTextLabel Text="{Loc 'rev-deconverted-rule'}" HorizontalAlignment="Center" />
                <RichTextLabel Text="{Loc 'rev-deconverted-ruletext'}" HorizontalAlignment="Center"/>
            </BoxContainer>
        </PanelContainer>
        <BoxContainer Orientation="Horizontal" Align="Center">
            <Button Name="ConfirmButton" Text="{Loc 'rev-deconverted-confirm'}"/>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
