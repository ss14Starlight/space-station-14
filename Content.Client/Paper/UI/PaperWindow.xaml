<paper:PaperWindow xmlns="https://spacestation14.io"
    xmlns:paper="clr-namespace:Content.Client.Paper.UI"
    MouseFilter="Stop" Resizable="True" MinSize="150 150"
    SetSize="666 660">

    <BoxContainer Name="ContentsRoot" Orientation="Vertical">
        <PanelContainer StyleClasses="BackgroundPanel" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="6">
            <TextureButton Name="CloseButton" StyleClasses="windowCloseButton"/>
        </PanelContainer>

        <!--
            Paper and tools share the same visual area, but ColorPanel is overlaid.
            This means showing the right-side drawing tools does NOT participate in layout
            and therefore does NOT squeeze the paper/document.
        -->
        <PanelContainer Name="PaperArea" VerticalExpand="True" HorizontalExpand="True">
            <PanelContainer Name="PaperBackground"
                            StyleIdentifier="PaperDefaultBorder"
                            HorizontalAlignment="Left"
                            VerticalExpand="True"
                            MinWidth="510"
                            MaxWidth="510">
                <ScrollContainer Name="ScrollingContents" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalExpand="True" VerticalExpand="True" HScrollEnabled="False">
                    <PanelContainer Name="PaperContent" VerticalExpand="True" HorizontalExpand="True" MaxWidth="600">
                        <paper:PaperDrawingControl Name="DrawingControl"
                                                   HorizontalExpand="True"
                                                   VerticalExpand="True"
                                                   MouseFilter="Ignore" />

                        <BoxContainer Orientation="Vertical" VerticalAlignment="Stretch">
                            <TextureButton Name="HeaderImage" HorizontalAlignment="Center" VerticalAlignment="Top" MouseFilter="Ignore"/>
                            <Control Name="TextAlignmentPadding" VerticalAlignment="Top"/>
                            <RichTextLabel Name="BlankPaperIndicator" StyleClasses="LabelWeak" VerticalAlignment="Top" HorizontalAlignment="Center"/>
                            <!-- Starlight-start -->
                            <RichTextLabel Name="WrittenTextLabel" VerticalAlignment="Top"/>
                            <BoxContainer Name="WrittenTextContainer" Orientation="Horizontal" VerticalAlignment="Top" StyleClasses="PaperWrittenText">
                            </BoxContainer>
                            <!-- Starlight-end -->
                            <BoxContainer Name="InputContainer" StyleIdentifier="PaperContainer" Orientation="Vertical" VerticalExpand="True" VerticalAlignment="Stretch">
                                <PanelContainer StyleIdentifier="PaperEditBackground" MinHeight="100"
                                    VerticalAlignment="Stretch" VerticalExpand="True" HorizontalExpand="True">
                                    <TextEdit Name="Input" StyleClasses="PaperLineEdit" Access="Public" />
                                </PanelContainer>
                                <Label Name="FillStatus" StyleClasses="LabelSecondaryColor"/>
                            </BoxContainer>
                            <TextureButton Name="FooterImage" HorizontalAlignment="Center" VerticalAlignment="Top" MouseFilter="Ignore"/>
                        </BoxContainer>

                        <paper:StampCollection Name="StampDisplay" VerticalAlignment="Bottom" Margin="6"/>
                    </PanelContainer>
                </ScrollContainer>
            </PanelContainer>

            <!--
                Right-side drawing panel.
                It is an overlay aligned to the right of the window, so it does not resize/squish PaperBackground.
            -->
            <PanelContainer Name="ColorPanel"
                            StyleClasses="BackgroundPanel"
                            MinWidth="150"
                            VerticalExpand="True"
                            HorizontalAlignment="Right">
                <BoxContainer Name="ColorButtons"
                              Orientation="Vertical"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Top"
                              Margin="6"
                              SeparationOverride="4" />
            </PanelContainer>
        </PanelContainer>

        <!-- Write/Draw/Clear/Save stay below the paper. -->
        <BoxContainer Name="EditButtons" Orientation="Horizontal" HorizontalAlignment="Right" Margin="6" SeparationOverride="4">
            <Button Name="WriteModeButton" Text="Write" />
            <Button Name="DrawModeButton" Text="Draw" />
            <Label Name="ThicknessLabel" Text="Thickness:" VerticalAlignment="Center" />
            <Button Name="Thickness1Button" Text="1" />
            <Button Name="Thickness2Button" Text="2" />
            <Button Name="Thickness3Button" Text="3" />
            <Button Name="Thickness4Button" Text="4" />
            <Button Name="Thickness5Button" Text="5" />
            <Button Name="ClearDrawingButton" Text="Clear Drawing" />
            <Button Name="SaveDrawingButton" Text="Save Drawing" />
            <Button Name="SaveButton" />
        </BoxContainer>
    </BoxContainer>
</paper:PaperWindow>
